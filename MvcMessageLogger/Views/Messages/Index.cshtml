@model MvcMessageLogger.Models.User

<h1>@Model.Name Messages</h1>

@foreach(var message in Model.Messages)
{
    <p>@message.CreatedAt: @message.Content</p>
}

<form method="get" action="/users/@Model.Id">
    <button type="submit">User Details</button>
</form>