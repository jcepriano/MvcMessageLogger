@model IEnumerable<MvcMessageLogger.Models.User>

    <h1>Users</h1>

    @foreach(var user in Model)
    {
        <p>Name: @user.Name, Username: @user.Username</p>
    }
    
    <a href="/users/new">Create a New Profile</a>